{
  "name": "@lambda-musika/audio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "pnpm run \"/^lint:.+$/\"",
    "lint:tsc": "tsc --build",
    "test": "vitest --typecheck",
    "clean": "rm -rf build dist",
    "clean:hard": "rm -rf build dist node_modules/.tmp/tsconfig.*.tsbuildinfo"
  },
  "dependencies": {},
  "devDependencies": {
    "type-fest": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
